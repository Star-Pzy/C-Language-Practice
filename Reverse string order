#include <stdio.h>

int main()
{
    char ch[10000] = "0";
    int i = 0;
    while ((ch[i] = getchar()) != '\n')
    {
        i++;
    }
    i -= 1;
    while (i != -1)
    {
        printf("%c", ch[i]);
        i--;
    }
    return 0;
}
